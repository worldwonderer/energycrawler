# Legacy/full install path (backward compatible).
# For capability-based install paths use:
#   - requirements-core.txt
#   - requirements-optional.txt
-r requirements-core.txt
-r requirements-optional.txt
